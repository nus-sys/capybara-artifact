diff --git a/eval/latency_cdf.sh b/eval/latency_cdf.sh
index cb6541e7..9e99002d 100644
--- a/eval/latency_cdf.sh
+++ b/eval/latency_cdf.sh
@@ -5,7 +5,7 @@ if [ $# -ne 1 ];then
 	exit 1
 fi
 
-exptid=/homes/inho/capybara-data/$1
+exptid=${CAPYBARA_DATA:-$HOME/capybara-data}/$1
 
 cat $exptid.latency_trace | awk -F',' '{print $3}' | sort -n | uniq --count | awk '
 BEGIN {
diff --git a/eval/mig_delay_cdf_avg_minmax_stddev.sh b/eval/mig_delay_cdf_avg_minmax_stddev.sh
index d2a028d1..c6624126 100644
--- a/eval/mig_delay_cdf_avg_minmax_stddev.sh
+++ b/eval/mig_delay_cdf_avg_minmax_stddev.sh
@@ -5,7 +5,7 @@ if [ $# -ne 1 ];then
 	exit 1
 fi
 
-exptid=/homes/inho/capybara-data/$1
+exptid=${CAPYBARA_DATA:-$HOME/capybara-data}/$1
 
 input_file=$exptid.mig_delay
 num_columns=$(head -1 $input_file | awk -F, '{print NF}')
diff --git a/eval/ms_avg_99p_lat.sh b/eval/ms_avg_99p_lat.sh
index 2ee23a1e..32284178 100644
--- a/eval/ms_avg_99p_lat.sh
+++ b/eval/ms_avg_99p_lat.sh
@@ -5,7 +5,7 @@ if [ $# -ne 1 ];then
 	exit 1
 fi
 
-exptid=/homes/inho/capybara-data/$1
+exptid=${CAPYBARA_DATA:-$HOME/capybara-data}/$1
 
 cat $exptid.latency_trace | awk -F, '                              
 {
diff --git a/eval/ms_total_even_odd_numreq.sh b/eval/ms_total_even_odd_numreq.sh
index a23d5adb..eb3bbc36 100644
--- a/eval/ms_total_even_odd_numreq.sh
+++ b/eval/ms_total_even_odd_numreq.sh
@@ -5,7 +5,7 @@ if [ $# -ne 1 ];then
 	exit 1
 fi
 
-exptid=/homes/inho/capybara-data/$1
+exptid=${CAPYBARA_DATA:-$HOME/capybara-data}/$1
 
 cat $exptid.latency_trace | awk -F, '                                              
 {
diff --git a/eval/parse_mig_cpu_ovhd.sh b/eval/parse_mig_cpu_ovhd.sh
index f73c6be0..1827ed60 100644
--- a/eval/parse_mig_cpu_ovhd.sh
+++ b/eval/parse_mig_cpu_ovhd.sh
@@ -5,7 +5,7 @@ if [ $# -ne 1 ];then
 	exit 1
 fi
 
-exptid=/homes/inho/capybara-data/$1
+exptid=${CAPYBARA_DATA:-$HOME/capybara-data}/$1
 
 cat $exptid.be0 | grep PROF | tail -200000 | awk -F, 'NR%2{val=$2; next} {sum=val+$2; print val "," $2 "," sum}' | awk -v exptid="$exptid" -F, '
 {
diff --git a/eval/parse_request_sched.sh b/eval/parse_request_sched.sh
index 3764a886..f8a9df1f 100644
--- a/eval/parse_request_sched.sh
+++ b/eval/parse_request_sched.sh
@@ -5,7 +5,7 @@ if [ $# -ne 1 ];then
 	exit 1
 fi
 
-exptid=/homes/inho/capybara-data/$1
+exptid=${CAPYBARA_DATA:-$HOME/capybara-data}/$1
 
 cat $exptid.request_sched | sort -t, -k1,1n |  awk -F, '
 BEGIN { first_val = 0; }
diff --git a/eval/parse_server_reply.sh b/eval/parse_server_reply.sh
index f1a53a1b..ceec9f63 100644
--- a/eval/parse_server_reply.sh
+++ b/eval/parse_server_reply.sh
@@ -5,7 +5,7 @@ if [ $# -ne 1 ];then
 	exit 1
 fi
 
-exptid=/homes/inho/capybara-data/$1
+exptid=${CAPYBARA_DATA:-$HOME/capybara-data}/$1
 
 cat $exptid.server_reply | sort -t, -k2,2n | awk -v exptid="$exptid" -F, '                              
 {
diff --git a/eval/workload_gap_cdf.sh b/eval/workload_gap_cdf.sh
index 3d962024..69327cff 100644
--- a/eval/workload_gap_cdf.sh
+++ b/eval/workload_gap_cdf.sh
@@ -5,7 +5,7 @@ if [ $# -ne 1 ];then
 	exit 1
 fi
 
-exptid=/homes/inho/capybara-data/$1
+exptid=${CAPYBARA_DATA:-$HOME/capybara-data}/$1
 
 paste -d, $exptid.be0_rps_signal $exptid.be1_rps_signal | awk -F, '{print ($3 - $6) < 0 ? -($3 - $6) : ($3 - $6)}' | sort -n | uniq --count | awk '
 BEGIN {
diff --git a/examples/rust/http-server.rs b/examples/rust/http-server.rs
index 8a3f7ecd..417a9a24 100644
--- a/examples/rust/http-server.rs
+++ b/examples/rust/http-server.rs
@@ -432,47 +432,47 @@ fn server(local: SocketAddrV4) -> Result<()> {
                     last_migration_time = Instant::now();
                     
                     /* COMMENT OUT THIS FOR APP_STATE_SIZE VS MIG_LAT EVAL */
-                    #[cfg(feature = "tcp-migration")]
-                    let state = if let Some(data) = libos.get_migrated_application_state::<ConnectionState>(new_qd) {
-                        server_log!("Connection State LOG: Received migrated app data ({} bytes)", data.borrow().serialized_size() - 8);
-                        data
-                    } else {
-                        server_log!("Connection State LOG: No migrated app data, creating new ConnectionState");
-                        let state = Rc::new(RefCell::new(ConnectionState {
-                            buffer: Buffer::new(),
-                            session_data: SessionData::new(session_data_size),
-                        }));
-                        libos.register_application_state(new_qd, state.clone());
-                        state
-                    };
-
-                    #[cfg(not(feature = "tcp-migration"))]
-                    let state = Rc::new(RefCell::new(ConnectionState {
-                        buffer: Buffer::new(),
-                        session_data: SessionData::new(session_data_size),
-                    }));
+                    // #[cfg(feature = "tcp-migration")]
+                    // let state = if let Some(data) = libos.get_migrated_application_state::<ConnectionState>(new_qd) {
+                        // server_log!("Connection State LOG: Received migrated app data ({} bytes)", data.borrow().serialized_size() - 8);
+                        // data
+                    // } else {
+                        // server_log!("Connection State LOG: No migrated app data, creating new ConnectionState");
+                        // let state = Rc::new(RefCell::new(ConnectionState {
+                            // buffer: Buffer::new(),
+                            // session_data: SessionData::new(session_data_size),
+                        // }));
+                        // libos.register_application_state(new_qd, state.clone());
+                        // state
+                    // };
+
+                    // #[cfg(not(feature = "tcp-migration"))]
+                    // let state = Rc::new(RefCell::new(ConnectionState {
+                        // buffer: Buffer::new(),
+                        // session_data: SessionData::new(session_data_size),
+                    // }));
                     // capy_time_log!("APP_STATE_REGISTERED,(n/a)");
                     
                     // Pop from new_qd
-                    match libos.pop(new_qd) {
-                        Ok(pop_qt) => {
-                            qts.push(pop_qt)
-                        },
-                        Err(e) => panic!("pop qt: {}", e),
-                    }
-                    #[cfg(feature = "manual-tcp-migration")]
-                    assert!(requests_remaining.insert(new_qd, migration_per_n).is_none());
-                    connstate.insert(new_qd, state);
+                    // match libos.pop(new_qd) {
+                        // Ok(pop_qt) => {
+                            // qts.push(pop_qt)
+                        // },
+                        // Err(e) => panic!("pop qt: {}", e),
+                    // }
+                    // #[cfg(feature = "manual-tcp-migration")]
+                    // assert!(requests_remaining.insert(new_qd, migration_per_n).is_none());
+                    // connstate.insert(new_qd, state);
                     /* COMMENT OUT THIS FOR APP_STATE_SIZE VS MIG_LAT EVAL */
 
 
                     /* ACTIVATE THIS FOR APP_STATE_SIZE VS MIG_LAT EVAL */
-                    // static mut NUM_MIG: u32 = 0;
-                    // unsafe{ NUM_MIG += 1; }
-                    // if unsafe{ NUM_MIG } < 11000 {
-                    //     libos.initiate_migration(new_qd).unwrap();
-                    // }
-                    // qts.push(libos.pop(new_qd).unwrap());
+                    static mut NUM_MIG: u32 = 0;
+                    unsafe{ NUM_MIG += 1; }
+                    if unsafe{ NUM_MIG } < 11000 {
+                        libos.initiate_migration(new_qd).unwrap();
+                    }
+                    qts.push(libos.pop(new_qd).unwrap());
                     /* ACTIVATE THIS FOR APP_STATE_SIZE VS MIG_LAT EVAL */
                     
                     // Re-arm accept
@@ -487,7 +487,13 @@ fn server(local: SocketAddrV4) -> Result<()> {
                 OperationResult::Pop(_, recvbuf) => {
                     server_log!("POP complete");
 
-                    let mut state = connstate.get_mut(&qd).unwrap().borrow_mut();
+                    let mut state = connstate
+                        .entry(qd)
+                        .or_insert_with(|| Rc::new(RefCell::new(ConnectionState {
+                            buffer: Buffer::new(),
+                            session_data: SessionData::new(session_data_size),
+                        })))
+                        .borrow_mut();
                     
                     let sent = push_data_and_run(&mut libos, qd, &mut state.buffer, &recvbuf, &mut qts);
 
@@ -554,6 +560,10 @@ fn server(local: SocketAddrV4) -> Result<()> {
                     match e.errno {
                         #[cfg(feature = "tcp-migration")]
                         demikernel::ETCPMIG => { server_log!("migrated {:?} polled", qd) },
+                        // The send buffer is full: the offered load is above what this
+                        // connection can drain. Drop the response and keep serving rather
+                        // than taking the whole server down.
+                        libc::EBUSY => { server_log!("send buffer full on {:?}", qd) },
                         _ => panic!("operation failed: {}", e),
                     }
                 }
diff --git a/examples/rust/https.rs b/examples/rust/https.rs
index de8557a5..649f6871 100644
--- a/examples/rust/https.rs
+++ b/examples/rust/https.rs
@@ -90,6 +90,7 @@ thread_local! {
 
 impl UserConnectionPeer for PeerData {
     fn migrate_in(remote: SocketAddrV4, buffer: Buffer) {
+        eprintln!("MIGDBG migrate_in len={}", buffer.len());
         let replaced = PEER.with_borrow_mut(|peer| peer.incoming.insert(remote, buffer));
         assert!(replaced.is_none())
     }
@@ -99,6 +100,7 @@ impl UserConnectionPeer for PeerData {
             .with_borrow_mut(|peer| peer.incoming.remove(&remote))
             .expect("exists incoming connection data");
         // let start = std::time::Instant::now(); 
+        eprintln!("MIGDBG import len={} head={:?}", buf.len(), &buf[..buf.len().min(8)]);
         let context = unsafe { tlse::tls_import_context(buf.as_ptr(), buf.len() as _) };
         // let duration = start.elapsed(); 
         // println!("tls_import_context time: {} microseconds", duration.as_nanos());
@@ -127,6 +129,7 @@ impl UserConnectionMigrateOut for MigrateOut {
     fn serialized_size(&self) -> usize {
         // let start = std::time::Instant::now(); // Start the timer
         let size = unsafe { tlse::tls_export_context(self.0, null_mut(), 0, 1) };
+        eprintln!("MIGDBG export size={} established={}", size, unsafe { tlse::tls_established(self.0) });
         // let duration = start.elapsed(); // Calculate the elapsed time
         // println!("tls_export_context1 time: {} microseconds", duration.as_nanos());
 
@@ -387,7 +390,7 @@ fn server(local: SocketAddrV4, migrate: bool) {
                     // server_log!("{:?}", &**recvbuf);
 
                     let context = PEER.with_borrow(|peer| peer.contexts[&qd]);
-                    unsafe {
+                    let consume_ret = unsafe {
                         tlse::tls_consume_stream(
                             context,
                             recvbuf.as_ptr(),
@@ -395,7 +398,9 @@ fn server(local: SocketAddrV4, migrate: bool) {
                             None,
                         )
                     };
-                    if unsafe { tlse::tls_established(context) } != 0 {
+                    eprintln!("MIGDBG consume ret={} established={} len={}",
+                        consume_ret, unsafe { tlse::tls_established(context) }, recvbuf.len());
+                    if unsafe { tlse::tls_established(context) } == 1 {
                         if migrate {
                             libos.initiate_migration(qd).expect("can migrate");
                         } else {
diff --git a/examples/rust/linux.mk b/examples/rust/linux.mk
index 9b004de0..b3bd3f3a 100644
--- a/examples/rust/linux.mk
+++ b/examples/rust/linux.mk
@@ -26,7 +26,7 @@ all: all-examples
 #	cp -f $(BUILD_DIR)/examples/capy-proxy-fe $(BINDIR)/examples/rust/capy-proxy-fe.$(EXEC_SUFFIX)
 #	cp -f $(BUILD_DIR)/examples/capy-proxy-be $(BINDIR)/examples/rust/capy-proxy-be.$(EXEC_SUFFIX)
 	cp -f $(BUILD_DIR)/examples/http-server $(BINDIR)/examples/rust/http-server.$(EXEC_SUFFIX)
-#	cp -f $(BUILD_DIR)/examples/https $(BINDIR)/examples/rust/https.$(EXEC_SUFFIX)
+	cp -f $(BUILD_DIR)/examples/https $(BINDIR)/examples/rust/https.$(EXEC_SUFFIX)
 #	cp -f $(BUILD_DIR)/examples/capybara-switch $(BINDIR)/examples/rust/capybara-switch.$(EXEC_SUFFIX)
 #	cp -f $(BUILD_DIR)/examples/proxy-server-fe $(BINDIR)/examples/rust/proxy-server-fe.$(EXEC_SUFFIX)
 #	cp -f $(BUILD_DIR)/examples/proxy-server-be $(BINDIR)/examples/rust/proxy-server-be.$(EXEC_SUFFIX)
@@ -49,15 +49,15 @@ all-examples:
 #	@echo "$(CARGO) build --example tcpmig-client $(CARGO_FEATURES) $(CARGO_FLAGS)"
 #	$(CARGO) build --example tcpmig-client $(CARGO_FEATURES) $(CARGO_FLAGS)
 #	@echo "$(CARGO) build --example dpdk-ctrl $(CARGO_FEATURES) $(CARGO_FLAGS)"
-#	$(CARGO) build --example dpdk-ctrl $(CARGO_FEATURES) $(CARGO_FLAGS)
+	$(CARGO) build --example dpdk-ctrl $(CARGO_FEATURES) $(CARGO_FLAGS) $(EXAMPLE_FEATURES)
 #	@echo "$(CARGO) build --example capy-proxy-fe $(CARGO_FEATURES) $(CARGO_FLAGS)"
 #	$(CARGO) build --example capy-proxy-fe $(CARGO_FEATURES) $(CARGO_FLAGS) $(EXAMPLE_FEATURES)
 #	@echo "$(CARGO) build --example capy-proxy-be $(CARGO_FEATURES) $(CARGO_FLAGS)"
 #	$(CARGO) build --example capy-proxy-be $(CARGO_FEATURES) $(CARGO_FLAGS) $(EXAMPLE_FEATURES)
 	@echo "$(CARGO) build --example http-server $(CARGO_FEATURES) $(CARGO_FLAGS)"
 	$(CARGO) build --example http-server $(CARGO_FEATURES) $(CARGO_FLAGS) $(EXAMPLE_FEATURES)
-#	@echo "$(CARGO) build --example https $(CARGO_FEATURES) $(CARGO_FLAGS)"
-#	$(CARGO) build --example https $(CARGO_FEATURES) $(CARGO_FLAGS) $(EXAMPLE_FEATURES)
+	@echo "$(CARGO) build --example https $(CARGO_FEATURES) $(CARGO_FLAGS)"
+	$(CARGO) build --example https $(CARGO_FEATURES) $(CARGO_FLAGS) $(EXAMPLE_FEATURES)
 #	$(CARGO) build --example capybara-switch $(CARGO_FEATURES) $(CARGO_FLAGS) $(EXAMPLE_FEATURES) --features=capybara-switch
 #,capy-profile
 #	$(CARGO) build --example proxy-server-fe $(CARGO_FEATURES) $(CARGO_FLAGS) $(EXAMPLE_FEATURES)
diff --git a/src/rust/catnip/runtime/memory/consts.rs b/src/rust/catnip/runtime/memory/consts.rs
index 4e894886..d103604a 100644
--- a/src/rust/catnip/runtime/memory/consts.rs
+++ b/src/rust/catnip/runtime/memory/consts.rs
@@ -18,10 +18,10 @@ use crate::runtime::libdpdk::{
 pub const DEFAULT_INLINE_BODY_SIZE: usize = 1024;
 
 /// Default number of buffers in the header pool.
-pub const DEFAULT_HEADER_POOL_SIZE: usize = 8192 - 1;
+pub const DEFAULT_HEADER_POOL_SIZE: usize = 8192 * 3 - 1;
 
 /// Default number of buffers in the body pool.
-pub const DEFAULT_BODY_POOL_SIZE: usize = 8192 - 1;
+pub const DEFAULT_BODY_POOL_SIZE: usize = 8192 * 3 - 1; /* fig10: jumbo multi-segment responses */
 
 /// Default value for maximum body size.
 pub const DEFAULT_MAX_BODY_SIZE: usize = (RTE_MBUF_DEFAULT_BUF_SIZE + RTE_PKTMBUF_HEADROOM) as usize;
diff --git a/src/rust/inetstack/protocols/tcp/established/sender.rs b/src/rust/inetstack/protocols/tcp/established/sender.rs
index 82211a91..6063375b 100644
--- a/src/rust/inetstack/protocols/tcp/established/sender.rs
+++ b/src/rust/inetstack/protocols/tcp/established/sender.rs
@@ -51,7 +51,7 @@ pub struct UnackedSegment {
 /// Hard limit for unsent queue.
 /// ToDo: Remove this.  We should limit the unsent queue by either having a (configurable) send buffer size (in bytes,
 /// not segments) and rejecting send requests that exceed that, or by limiting the user's send buffer allocations.
-const UNSENT_QUEUE_CUTOFF: usize = 1024 * 8;
+const UNSENT_QUEUE_CUTOFF: usize = 1024 * 64; /* fig10: multi-segment responses fill this 2-3x faster */
 
 // ToDo: Consider moving retransmit timer and congestion control fields out of this structure.
 // ToDo: Make all public fields in this structure private.
diff --git a/src/rust/inetstack/protocols/tcpmig/active.rs b/src/rust/inetstack/protocols/tcpmig/active.rs
index 0a109091..512d4f00 100644
--- a/src/rust/inetstack/protocols/tcpmig/active.rs
+++ b/src/rust/inetstack/protocols/tcpmig/active.rs
@@ -295,7 +295,7 @@ impl ActiveMigration {
             self.client
         );
         capy_time_log!("SEND_PREPARE_MIG,({}),[{}->{}:{}](via switch)", self.client, self.origin, self.remote_ipv4_addr, self.dest_udp_port);
-        self.send_via_switch(tcpmig_hdr, Buffer::Heap(DataBuffer::empty()));
+        self.send(tcpmig_hdr, Buffer::Heap(DataBuffer::empty()));
     }
 
     pub fn send_connection_state(&mut self, state: TcpState, user_data: MigrateOut) {
@@ -351,9 +351,9 @@ impl ActiveMigration {
             // PREPARE_MIG: Always send to switch
             capy_log_mig!("[SEND] PREPARE_MIG via switch: {} -> {}", self.remote_ipv4_addr, SWITCH_IP);
             (SWITCH_IP, SWITCH_MAC)
-        } else if self.remote_ipv4_addr == self.local_ipv4_addr {
-            // Same IP: route via switch
-            capy_log_mig!("[SEND] Same IP, routing via switch: {} -> {}", self.remote_ipv4_addr, SWITCH_IP);
+        } else if false {
+            // Same-address targets are delivered directly by MAC: the switch
+            // hairpins the frame back out the same port.
             (SWITCH_IP, SWITCH_MAC)
         } else {
             // Different IP: send directly
diff --git a/src/rust/inetstack/protocols/tcpmig/peer.rs b/src/rust/inetstack/protocols/tcpmig/peer.rs
index 882d04ca..3a9ecbed 100644
--- a/src/rust/inetstack/protocols/tcpmig/peer.rs
+++ b/src/rust/inetstack/protocols/tcpmig/peer.rs
@@ -155,7 +155,7 @@ impl TcpMigPeer {
         // TEMP: 2 servers for migration test (node8:10000 -> node9:10000)
         let backend_servers = arrayvec::ArrayVec::from([
             (NODE9_MAC, SocketAddrV4::new(Ipv4Addr::new(10, 0, 1, 9), 10000)),
-            (NODE8_MAC, SocketAddrV4::new(Ipv4Addr::new(10, 0, 1, 8), 10000)),
+            (NODE9_MAC, SocketAddrV4::new(Ipv4Addr::new(10, 0, 1, 9), 10001)),
         ]);
         
         
@@ -229,6 +229,7 @@ impl TcpMigPeer {
         thread_local! {
             static COUNT: Cell<u32> = Cell::new(0);
             static MIGRATE_AFTER: OnceCell<Option<u32>> = OnceCell::new();
+            static MIGRATE_EVERY: OnceCell<u32> = OnceCell::new();
         }
         let Some(migrate_after) = MIGRATE_AFTER.with(|migrate_after| {
             migrate_after
@@ -237,10 +238,18 @@ impl TcpMigPeer {
         }) else {
             return false;
         };
+        let every = MIGRATE_EVERY.with(|e| {
+            *e.get_or_init(|| {
+                std::env::var("MIG_EVERY")
+                    .ok()
+                    .and_then(|n| n.parse().ok())
+                    .filter(|n| *n > 0)
+                    .unwrap_or(1)
+            })
+        });
         let count = COUNT.get();
         COUNT.set(count + 1);
-        // eprintln!("count: {}", count);
-        count >= migrate_after
+        count >= migrate_after && count % every == 0
         
         // return count >= migrate_after && count % 50 == 0; // for maintenance eval
         
@@ -428,17 +437,24 @@ impl TcpMigPeer {
     pub fn initiate_migration(&mut self, conn: (SocketAddrV4, SocketAddrV4), qd: QDesc) {
         let (local, remote) = conn;
 
-        // Always send PREPARE_MIG to switch - switch will decide the target
+        // Address the prepare to the partner server directly, as the stack did at
+        // the paper's migration-latency runs: the two-entry backend table below
+        // carries the partner's address and MAC, and the switch only forwards.
+        let (target_mac, target_addr) = *self
+            .backend_servers
+            .iter()
+            .find(|(_, a)| !(*a.ip() == *local.ip() && a.port() == local.port()))
+            .expect("no partner server in backend_servers");
         capy_time_log!("INIT_MIG,({}),[{}->switch]", remote, local);
 
         let active = ActiveMigration::new(
             self.rt.clone(),
             self.local_ipv4_addr,
             self.local_link_addr,
-            SWITCH_IP,
-            SWITCH_MAC,
+            *target_addr.ip(),
+            target_mac,
             self.self_udp_port,
-            DEST_UDP_PORT,  // Switch UDP port
+            target_addr.port(),
             local,
             remote,
             Some(qd),
